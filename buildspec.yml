version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/SpringBootExample-2.1.2.RELEASE.jar
    - appspec.yml
  discard-paths: yes